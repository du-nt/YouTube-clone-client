import React from "react";
import { Typography } from "@material-ui/core";
import { Container } from "@material-ui/core";

export default function Private() {
  return (
    <Container maxWidth="md">
      <Typography variant="h5">Private page</Typography>
    </Container>
  );
}
